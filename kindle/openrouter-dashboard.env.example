# Copy to /mnt/us/openrouter-dashboard.env on the Kindle.
# Replace placeholder with your public or pre-signed object-storage URL.
DASHBOARD_URL='https://<OBJECT_STORAGE_HOST>/<PATH>/openrouter-dashboard.png'
FBINK='/usr/bin/fbink'
INTERVAL='21600'
FULL_EVERY='1'
WIFI_RETRY_EVERY='3'
MAX_FAILURES='3'
